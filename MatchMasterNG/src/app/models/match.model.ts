export interface Match {
    matchId?: number;
    tournamentId: number;
    startingMatch?: boolean;
    winnerId?: number;
    matchTitle: string;
    matchStart: Date;
    winMatch?: number;
    loseMatch?: number;
  }
  